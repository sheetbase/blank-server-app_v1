/* tslint:disable:no-unused-expression */
import { expect } from 'chai';
import { describe, it } from 'mocha';

import { Sheetbase } from '../src/index';

const options = Sheetbase.Option.get();

describe('Test me', () => {
    it('should do something');
});
